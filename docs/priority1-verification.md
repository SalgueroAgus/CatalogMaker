# Priority 1 verification record

Status: **incomplete—awaiting verification**. All 406 automated cases have current passing evidence from FULL400 plus affected S3 reruns for the relevant final source. Required actual-device and both-parent evidence remains unavailable; the overall Priority 1 gate is not approved.

## Fixed scope and preserved starting state

Scope fixed before source edits on 2026-09-12: D1, D2, A3, A2, U2, U3, U5, L1 and affected A5/Q1/Q2/Q3. Priority 2–4, IDEAS work, catalog identity/copies/migration/history/backup, publishing changes and dependency upgrades are excluded. A3 includes only current catalog and session retry/export ownership. Q2 excludes experiments and returning to Principal.

Starting HEAD: `cc461feaadb80d56a5b22abb1a6fc3fd6d7baeca`. Initial status: unstaged modifications to `ROADMAP.md` and `IDEAS.md`; no staged changes or untracked files. Complete binary diffs, original documents, tracked SHA-256 inventory, objective and status were recorded in `/private/tmp/catalogmaker-priority1-baseline/`. Task review compares against these originals, not against HEAD for those two documents.

Baseline `npm run verify`: exit 0 (TypeScript, Vite, unstaged/staged whitespace). Existing warnings: Vite CJS Node API deprecation and bundle size above 500 kB. No baseline behavior suite or CI workflow exists. Netlify builds with `npm run build`; no deployment invoked. Inspected package scripts, lockfile, installed idb-keyval 6.2.5 transaction implementation, README, architecture, AGENTS, working-tree roadmap, review skill and relevant callers.

## Evidence rules

Statuses: pending, implemented/unverified, failed, blocked, verified. Each row requires current source fingerprint, command/manual procedure, actual vs expected outcome and artifact references before becoming verified. Build is not browser/output evidence. Emulation is not actual-device or parent evidence. Artifact files go outside tracked source. No publishing requests are permitted in tests.

## Acceptance checklist

### D1 — Honest saving and loading

| Criterion | Requirement | Status | Evidence / next step |
|---|---|---|---|
| D1.1 | Gate all mutation entry points until a coherent hydration result is available. | verified | FULL400: persistence.spec.ts; S3: export-recovery/persistence-additional. Real delayed/failing IDB, latest revision, retry/reload, and inspected failed-draft PDF/HTML with retained photo/text. |
| D1.2 | Distinguish loading, successfully loaded empty data, and failed loading. | verified | FULL400: persistence.spec.ts; S3: export-recovery/persistence-additional. Real delayed/failing IDB, latest revision, retry/reload, and inspected failed-draft PDF/HTML with retained photo/text. |
| D1.3 | Provide actionable retry. | verified | FULL400: persistence.spec.ts; S3: export-recovery/persistence-additional. Real delayed/failing IDB, latest revision, retry/reload, and inspected failed-draft PDF/HTML with retained photo/text. |
| D1.4 | Show “Guardando…”, “Guardado en este navegador”, or an actionable save failure. | verified | FULL400: persistence.spec.ts; S3: export-recovery/persistence-additional. Real delayed/failing IDB, latest revision, retry/reload, and inspected failed-draft PDF/HTML with retained photo/text. |
| D1.5 | The indicator covers product metadata/order, product photos, settings/layouts, and background images. | verified | FULL400: persistence.spec.ts; S3: export-recovery/persistence-additional. Real delayed/failing IDB, latest revision, retry/reload, and inspected failed-draft PDF/HTML with retained photo/text. |
| D1.6 | It may report saved only after all writes needed for the latest displayed state have committed. | verified | FULL400: persistence.spec.ts; S3: export-recovery/persistence-additional. Real delayed/failing IDB, latest revision, retry/reload, and inspected failed-draft PDF/HTML with retained photo/text. |
| D1.7 | An older successful write must not hide newer pending or failed edits. | verified | FULL400: persistence.spec.ts; S3: export-recovery/persistence-additional. Real delayed/failing IDB, latest revision, retry/reload, and inspected failed-draft PDF/HTML with retained photo/text. |
| D1.8 | Retain failed edits and necessary image resources for retry and existing PDF export while the session remains open. | verified | FULL400: persistence.spec.ts; S3: export-recovery/persistence-additional. Real delayed/failing IDB, latest revision, retry/reload, and inspected failed-draft PDF/HTML with retained photo/text. |
| D1.9 | Explain unsaved state honestly; persistent recovery across closing the browser is not a new Priority 1 feature. | verified | FULL400: persistence.spec.ts; S3: export-recovery/persistence-additional. Real delayed/failing IDB, latest revision, retry/reload, and inspected failed-draft PDF/HTML with retained photo/text. |
| D1.10 | Prove with delayed hydration, StrictMode startup, failed reads, failed/aborted/quota-rejected writes in every persistence domain, rapid overlapping edits, retry followed by more editing, and immediate reload after confirmed saving. | verified | FULL400: persistence.spec.ts; S3: export-recovery/persistence-additional. Real delayed/failing IDB, latest revision, retry/reload, and inspected failed-draft PDF/HTML with retained photo/text. |
| D1.11 | Also reload during pending writes: it must not leave mixed metadata/images or corrupt the previous durable state. | verified | FULL400: persistence.spec.ts; S3: export-recovery/persistence-additional. Real delayed/failing IDB, latest revision, retry/reload, and inspected failed-draft PDF/HTML with retained photo/text. |
| D1.12 | Do not claim an uncommitted edit is guaranteed to survive forced reload. | verified | FULL400: persistence.spec.ts; S3: export-recovery/persistence-additional. Real delayed/failing IDB, latest revision, retry/reload, and inspected failed-draft PDF/HTML with retained photo/text. |
### D2 — Separate destructive operations

| Criterion | Requirement | Status | Evidence / next step |
|---|---|---|---|
| D2.1 | Remove destructive actions from routine export controls. | verified | FULL400: persistence.spec.ts; S3: persistence-additional. All three effects, confirmation/cancel, delayed/failed/repeated reset, queued writes, subsequent add, stored metadata/photo-byte comparisons and background-tab reload. |
| D2.2 | Provide three distinct effects: | verified | FULL400: persistence.spec.ts; S3: persistence-additional. All three effects, confirmation/cancel, delayed/failed/repeated reset, queued writes, subsequent add, stored metadata/photo-byte comparisons and background-tab reload. |
| D2.3 | \- Empty catalog: delete products and their photos; preserve settings/background. | verified | FULL400: persistence.spec.ts; S3: persistence-additional. All three effects, confirmation/cancel, delayed/failed/repeated reset, queued writes, subsequent add, stored metadata/photo-byte comparisons and background-tab reload. |
| D2.4 | \- Reset settings: restore settings, layout choices, and background defaults; preserve all products, product metadata/order, and product photos. | verified | FULL400: persistence.spec.ts; S3: persistence-additional. All three effects, confirmation/cancel, delayed/failed/repeated reset, queued writes, subsequent add, stored metadata/photo-byte comparisons and background-tab reload. |
| D2.5 | \- Reset everything: delete current catalog data and restore defaults. | verified | FULL400: persistence.spec.ts; S3: persistence-additional. All three effects, confirmation/cancel, delayed/failed/repeated reset, queued writes, subsequent add, stored metadata/photo-byte comparisons and background-tab reload. |
| D2.6 | Confirmations must identify the action, current catalog using its available display name, and the effect. | verified | FULL400: persistence.spec.ts; S3: persistence-additional. All three effects, confirmation/cancel, delayed/failed/repeated reset, queued writes, subsequent add, stored metadata/photo-byte comparisons and background-tab reload. |
| D2.7 | Cancellation changes nothing. | verified | FULL400: persistence.spec.ts; S3: persistence-additional. All three effects, confirmation/cancel, delayed/failed/repeated reset, queued writes, subsequent add, stored metadata/photo-byte comparisons and background-tab reload. |
| D2.8 | Serialize destructive operations with pending writes and await completion. | verified | FULL400: persistence.spec.ts; S3: persistence-additional. All three effects, confirmation/cancel, delayed/failed/repeated reset, queued writes, subsequent add, stored metadata/photo-byte comparisons and background-tab reload. |
| D2.9 | Adding products immediately after reset must not race cleanup. | verified | FULL400: persistence.spec.ts; S3: persistence-additional. All three effects, confirmation/cancel, delayed/failed/repeated reset, queued writes, subsequent add, stored metadata/photo-byte comparisons and background-tab reload. |
| D2.10 | Failures must remain visible and recoverable. | verified | FULL400: persistence.spec.ts; S3: persistence-additional. All three effects, confirmation/cancel, delayed/failed/repeated reset, queued writes, subsequent add, stored metadata/photo-byte comparisons and background-tab reload. |
| D2.11 | Prove each action after reload, including cancellation, delayed cleanup, failed cleanup, rapid repeated activation, and reset followed immediately by adding a product. | verified | FULL400: persistence.spec.ts; S3: persistence-additional. All three effects, confirmation/cancel, delayed/failed/repeated reset, queued writes, subsequent add, stored metadata/photo-byte comparisons and background-tab reload. |
| D2.12 | Compare stored product metadata and photo bytes across settings-only reset. | verified | FULL400: persistence.spec.ts; S3: persistence-additional. All three effects, confirmation/cancel, delayed/failed/repeated reset, queued writes, subsequent add, stored metadata/photo-byte comparisons and background-tab reload. |
| D2.13 | Prove that switching background tabs retains the image through reload, while explicit removal persists. | verified | FULL400: persistence.spec.ts; S3: persistence-additional. All three effects, confirmation/cancel, delayed/failed/repeated reset, queued writes, subsequent add, stored metadata/photo-byte comparisons and background-tab reload. |
### A3 — Image and write ownership

| Criterion | Requirement | Status | Evidence / next step |
|---|---|---|---|
| A3.1 | Treat metadata and associated image changes as one recoverable user action with atomic persistence or an equivalently demonstrated recovery guarantee. | verified | FULL400: persistence/ownership; S3: ownership/persistence-additional/export-recovery. Real transactions, URL allocation/revocation, durable/draft decoding, partial hydration cleanup, retained export owners and capture failure cleanup. |
| A3.2 | Handle every database rejection. | verified | FULL400: persistence/ownership; S3: ownership/persistence-additional/export-recovery. Real transactions, URL allocation/revocation, durable/draft decoding, partial hydration cleanup, retained export owners and capture failure cleanup. |
| A3.3 | Define owners for active, durable, pending/retry, hydration, and export resources. | verified | FULL400: persistence/ownership; S3: ownership/persistence-additional/export-recovery. Real transactions, URL allocation/revocation, durable/draft decoding, partial hydration cleanup, retained export owners and capture failure cleanup. |
| A3.4 | Release obsolete object URLs after their final consumer releases them. | verified | FULL400: persistence/ownership; S3: ownership/persistence-additional/export-recovery. Real transactions, URL allocation/revocation, durable/draft decoding, partial hydration cleanup, retained export owners and capture failure cleanup. |
| A3.5 | Reclaim unused or stale hydration results, including partially failed loads and late results. | verified | FULL400: persistence/ownership; S3: ownership/persistence-additional/export-recovery. Real transactions, URL allocation/revocation, durable/draft decoding, partial hydration cleanup, retained export owners and capture failure cleanup. |
| A3.6 | Delete stored blobs only when no current owner needs them. | verified | FULL400: persistence/ownership; S3: ownership/persistence-additional/export-recovery. Real transactions, URL allocation/revocation, durable/draft decoding, partial hydration cleanup, retained export owners and capture failure cleanup. |
| A3.7 | Preserve the old durable photo if replacement fails, and retain the new draft when needed for retry. | verified | FULL400: persistence/ownership; S3: ownership/persistence-additional/export-recovery. Real transactions, URL allocation/revocation, durable/draft decoding, partial hydration cleanup, retained export owners and capture failure cleanup. |
| A3.8 | Invalid replacement IDs must not allocate URLs or orphan blobs. | verified | FULL400: persistence/ownership; S3: ownership/persistence-additional/export-recovery. Real transactions, URL allocation/revocation, durable/draft decoding, partial hydration cleanup, retained export owners and capture failure cleanup. |
| A3.9 | Prove with injected failures during add/import/replace/delete/reset, repeated background replacement/removal, stale hydration, and repeated export. | verified | FULL400: persistence/ownership; S3: ownership/persistence-additional/export-recovery. Real transactions, URL allocation/revocation, durable/draft decoding, partial hydration cleanup, retained export owners and capture failure cleanup. |
| A3.10 | Instrument URL allocation/revocation and inspect database contents; verify retained images still decode and obsolete resources are released after operations settle. | verified | FULL400: persistence/ownership; S3: ownership/persistence-additional/export-recovery. Real transactions, URL allocation/revocation, durable/draft decoding, partial hydration cleanup, retained export owners and capture failure cleanup. |
| A3.11 | Do not build hypothetical multi-catalog or snapshot infrastructure. | verified | FULL400: persistence/ownership; S3: ownership/persistence-additional/export-recovery. Real transactions, URL allocation/revocation, durable/draft decoding, partial hydration cleanup, retained export owners and capture failure cleanup. |
### A2 — Safe identity and mutations

| Criterion | Requirement | Status | Evidence / next step |
|---|---|---|---|
| A2.1 | Use collision-resistant IDs in blank-product creation, photo uploads/drop intake, and Excel import. | verified | FULL400: mutations-pagination/ownership; S3: ownership. Fixed-clock mixed intake, unique IDs, no-op/missing-ID guards, every field entry path, actual Excel correction and preservation of legacy stored descriptions. |
| A2.2 | Preserve existing valid stored identities. | verified | FULL400: mutations-pagination/ownership; S3: ownership. Fixed-clock mixed intake, unique IDs, no-op/missing-ID guards, every field entry path, actual Excel correction and preservation of legacy stored descriptions. |
| A2.3 | Missing IDs, self-reorders, empty lists, and boundary moves must safely leave state and storage unchanged. | verified | FULL400: mutations-pagination/ownership; S3: ownership. Fixed-clock mixed intake, unique IDs, no-op/missing-ID guards, every field entry path, actual Excel correction and preservation of legacy stored descriptions. |
| A2.4 | Cover missing source and destination IDs independently. | verified | FULL400: mutations-pagination/ownership; S3: ownership. Fixed-clock mixed intake, unique IDs, no-op/missing-ID guards, every field entry path, actual Excel correction and preservation of legacy stored descriptions. |
| A2.5 | Centralize editable fields and validation. | verified | FULL400: mutations-pagination/ownership; S3: ownership. Fixed-clock mixed intake, unique IDs, no-op/missing-ID guards, every field entry path, actual Excel correction and preservation of legacy stored descriptions. |
| A2.6 | List editing, preview editing, and import must agree on accepted field values and description limits. | verified | FULL400: mutations-pagination/ownership; S3: ownership. Fixed-clock mixed intake, unique IDs, no-op/missing-ID guards, every field entry path, actual Excel correction and preservation of legacy stored descriptions. |
| A2.7 | Preserve existing stored content when loading. | verified | FULL400: mutations-pagination/ownership; S3: ownership. Fixed-clock mixed intake, unique IDs, no-op/missing-ID guards, every field entry path, actual Excel correction and preservation of legacy stored descriptions. |
| A2.8 | Prove with rapid mixed add/import operations while the clock is fixed, unique-ID assertions, invalid-ID mutations, reorder boundaries, and field-limit tests through every entry point. | verified | FULL400: mutations-pagination/ownership; S3: ownership. Fixed-clock mixed intake, unique IDs, no-op/missing-ID guards, every field entry path, actual Excel correction and preservation of legacy stored descriptions. |
| A2.9 | Verify that invalid operations cause neither array corruption nor orphaned images. | verified | FULL400: mutations-pagination/ownership; S3: ownership. Fixed-clock mixed intake, unique IDs, no-op/missing-ID guards, every field entry path, actual Excel correction and preservation of legacy stored descriptions. |
### U2 — Readable, explicit, accessible controls

| Criterion | Requirement | Status | Evidence / next step |
|---|---|---|---|
| U2.1 | Provide persistent field labels in the editor, associated labels/accessibly named preview editing controls, visible “Cambiar foto”, and understandable action names. | verified | FULL400 and S3: ui/contrast/secondary-controls/accessibility-extra/zoom. Associated labels and semantic states, keyboard workflow, measured 16px inputs/44px primary targets, secondary targets and contrast, actual Chromium 200% zoom; family assessment separately blocked. |
| U2.2 | Replace clickable noninteractive elements with buttons/links where they perform actions. | verified | FULL400 and S3: ui/contrast/secondary-controls/accessibility-extra/zoom. Associated labels and semantic states, keyboard workflow, measured 16px inputs/44px primary targets, secondary targets and contrast, actual Chromium 200% zoom; family assessment separately blocked. |
| U2.3 | Expose selected, expanded, disabled, and error states semantically. | verified | FULL400 and S3: ui/contrast/secondary-controls/accessibility-extra/zoom. Associated labels and semantic states, keyboard workflow, measured 16px inputs/44px primary targets, secondary targets and contrast, actual Chromium 200% zoom; family assessment separately blocked. |
| U2.4 | Essential operations must work without hover or color alone. | verified | FULL400 and S3: ui/contrast/secondary-controls/accessibility-extra/zoom. Associated labels and semantic states, keyboard workflow, measured 16px inputs/44px primary targets, secondary targets and contrast, actual Chromium 200% zoom; family assessment separately blocked. |
| U2.5 | Keyboard focus must remain visible and reachable; hidden controls must not receive focus. | verified | FULL400 and S3: ui/contrast/secondary-controls/accessibility-extra/zoom. Associated labels and semantic states, keyboard workflow, measured 16px inputs/44px primary targets, secondary targets and contrast, actual Chromium 200% zoom; family assessment separately blocked. |
| U2.6 | Use at least 16 CSS px text in editor inputs and at least 44×44 CSS px primary action targets. | verified | FULL400 and S3: ui/contrast/secondary-controls/accessibility-extra/zoom. Associated labels and semantic states, keyboard workflow, measured 16px inputs/44px primary targets, secondary targets and contrast, actual Chromium 200% zoom; family assessment separately blocked. |
| U2.7 | Primary actions include add, replace photo, move, delete, navigation, export, and reset. | verified | FULL400 and S3: ui/contrast/secondary-controls/accessibility-extra/zoom. Associated labels and semantic states, keyboard workflow, measured 16px inputs/44px primary targets, secondary targets and contrast, actual Chromium 200% zoom; family assessment separately blocked. |
| U2.8 | Assess secondary target sizing/spacing using the W3C target-size guidance linked from the roadmap. | verified | FULL400 and S3: ui/contrast/secondary-controls/accessibility-extra/zoom. Associated labels and semantic states, keyboard workflow, measured 16px inputs/44px primary targets, secondary targets and contrast, actual Chromium 200% zoom; family assessment separately blocked. |
| U2.9 | Require editor text contrast of at least 4.5:1, or 3:1 for qualifying large text, and at least 3:1 for visual information needed to identify controls/states. | verified | FULL400 and S3: ui/contrast/secondary-controls/accessibility-extra/zoom. Associated labels and semantic states, keyboard workflow, measured 16px inputs/44px primary targets, secondary targets and contrast, actual Chromium 200% zoom; family assessment separately blocked. |
| U2.10 | These are scoped acceptance checks, not a claim of whole-app WCAG certification. | verified | FULL400 and S3: ui/contrast/secondary-controls/accessibility-extra/zoom. Associated labels and semantic states, keyboard workflow, measured 16px inputs/44px primary targets, secondary targets and contrast, actual Chromium 200% zoom; family assessment separately blocked. |
| U2.11 | At 200% actual desktop browser zoom, all essential controls and entered values remain usable. | verified | FULL400 and S3: ui/contrast/secondary-controls/accessibility-extra/zoom. Associated labels and semantic states, keyboard workflow, measured 16px inputs/44px primary targets, secondary targets and contrast, actual Chromium 200% zoom; family assessment separately blocked. |
| U2.12 | Preserve compact A4 typography and page geometry separately; editor-only labels and actions must not appear in exported catalogs. | verified | FULL400 and S3: ui/contrast/secondary-controls/accessibility-extra/zoom. Associated labels and semantic states, keyboard workflow, measured 16px inputs/44px primary targets, secondary targets and contrast, actual Chromium 200% zoom; family assessment separately blocked. |
| U2.13 | Prove with computed-style/target measurements, label and selected-state assertions, keyboard-only task walkthroughs, contrast measurements, zoom checks, screenshots, and the family assessment below. | blocked | FULL400 and S3: ui/contrast/secondary-controls/accessibility-extra/zoom. Associated labels and semantic states, keyboard workflow, measured 16px inputs/44px primary targets, secondary targets and contrast, actual Chromium 200% zoom; family assessment separately blocked. Missing real-device/parent proof is not a pass. |
### U3 — Reorder without dragging

| Criterion | Requirement | Status | Evidence / next step |
|---|---|---|---|
| U3.1 | Expose labeled “Subir” and “Bajar” controls using moveProduct. | verified | FULL400 and S3: keyboard moves, native desktop drag events, focus and saved reload; export-recovery actual reordered/shrinking-ref PDF agrees with index and preview. |
| U3.2 | Disable unavailable boundary directions, support keyboard activation, and preserve useful focus after movement. | verified | FULL400 and S3: keyboard moves, native desktop drag events, focus and saved reload; export-recovery actual reordered/shrinking-ref PDF agrees with index and preview. |
| U3.3 | Keep desktop drag reorder working. | verified | FULL400 and S3: keyboard moves, native desktop drag events, focus and saved reload; export-recovery actual reordered/shrinking-ref PDF agrees with index and preview. |
| U3.4 | Prove first/middle/last and single-product cases, repeated moves, desktop dragging, persisted order after reload, and agreement between product list, index, preview, and output. | verified | FULL400 and S3: keyboard moves, native desktop drag events, focus and saved reload; export-recovery actual reordered/shrinking-ref PDF agrees with index and preview. |
### U5 — Mobile viewport and editing

| Criterion | Requirement | Status | Evidence / next step |
|---|---|---|---|
| U5.1 | Correct vh/dvh fallback order and preserve bottom safe-area subtraction in the effective layout rules. | verified | FULL400 and S3: ui/accessibility-extra/secondary-controls. Seven phone/tablet/desktop viewport sizes, all tabs, drawer focus/close, last action and reduced-height resizing. Source confirms vh before dvh and effective safe-area subtraction; actual-device observations remain blocked. |
| U5.2 | Provide readable editing through the Products panel independently of scaled A4 text. | verified | FULL400 and S3: ui/accessibility-extra/secondary-controls. Seven phone/tablet/desktop viewport sizes, all tabs, drawer focus/close, last action and reduced-height resizing. Source confirms vh before dvh and effective safe-area subtraction; actual-device observations remain blocked. |
| U5.3 | Verify all mobile tabs and the tablet drawer, including reachable opening/closing controls and keyboard focus. | verified | FULL400 and S3: ui/accessibility-extra/secondary-controls. Seven phone/tablet/desktop viewport sizes, all tabs, drawer focus/close, last action and reduced-height resizing. Source confirms vh before dvh and effective safe-area subtraction; actual-device observations remain blocked. |
| U5.4 | Prove portrait/landscape behavior, keyboard open/closed, bottom safe areas, scrolling to the last product/action, viewport resizing, and breakpoint transitions. | blocked | FULL400 and S3: ui/accessibility-extra/secondary-controls. Seven phone/tablet/desktop viewport sizes, all tabs, drawer focus/close, last action and reduced-height resizing. Source confirms vh before dvh and effective safe-area subtraction; actual-device observations remain blocked. Missing real-device/parent proof is not a pass. |
| U5.5 | Reduced-height emulation alone does not prove real mobile keyboard behavior. | verified | FULL400 and S3: ui/accessibility-extra/secondary-controls. Seven phone/tablet/desktop viewport sizes, all tabs, drawer focus/close, last action and reduced-height resizing. Source confirms vh before dvh and effective safe-area subtraction; actual-device observations remain blocked. |
### L1 — Page numbers and backgrounds

| Criterion | Requirement | Status | Evidence / next step |
|---|---|---|---|
| L1.1 | Use the actual index-page count everywhere. | verified | FULL400 plus S3 rerun of the interrupted 1×5 case: 30 DOM count combinations, 75 real PDF/HTML boundary combinations, 63 supported-shape output cases and 12 repeated-output opacity cases. PDFKit/Vision numbering, A4 geometry, links, pixels and partial last pages; relevant pagination/capture/style source unchanged in S3. |
| L1.2 | For N > 0: | verified | FULL400 plus S3 rerun of the interrupted 1×5 case: 30 DOM count combinations, 75 real PDF/HTML boundary combinations, 63 supported-shape output cases and 12 repeated-output opacity cases. PDFKit/Vision numbering, A4 geometry, links, pixels and partial last pages; relevant pagination/capture/style source unchanged in S3. |
| L1.3 | \- Index pages = ceil(N / 30). | verified | FULL400 plus S3 rerun of the interrupted 1×5 case: 30 DOM count combinations, 75 real PDF/HTML boundary combinations, 63 supported-shape output cases and 12 repeated-output opacity cases. PDFKit/Vision numbering, A4 geometry, links, pixels and partial last pages; relevant pagination/capture/style source unchanged in S3. |
| L1.4 | \- Product at zero-based position j appears on floor(j / itemsPerPage) + index-page count + 1. | verified | FULL400 plus S3 rerun of the interrupted 1×5 case: 30 DOM count combinations, 75 real PDF/HTML boundary combinations, 63 supported-shape output cases and 12 repeated-output opacity cases. PDFKit/Vision numbering, A4 geometry, links, pixels and partial last pages; relevant pagination/capture/style source unchanged in S3. |
| L1.5 | \- Total pages = index-page count + ceil(N / itemsPerPage). | verified | FULL400 plus S3 rerun of the interrupted 1×5 case: 30 DOM count combinations, 75 real PDF/HTML boundary combinations, 63 supported-shape output cases and 12 repeated-output opacity cases. PDFKit/Vision numbering, A4 geometry, links, pixels and partial last pages; relevant pagination/capture/style source unchanged in S3. |
| L1.6 | An empty catalog renders no catalog pages and cannot export phantom pages. | verified | FULL400 plus S3 rerun of the interrupted 1×5 case: 30 DOM count combinations, 75 real PDF/HTML boundary combinations, 63 supported-shape output cases and 12 repeated-output opacity cases. PDFKit/Vision numbering, A4 geometry, links, pixels and partial last pages; relevant pagination/capture/style source unchanged in S3. |
| L1.7 | Page-list labels, product-list separators, preview numbering, index destinations, page refs, and exported links must agree. | verified | FULL400 plus S3 rerun of the interrupted 1×5 case: 30 DOM count combinations, 75 real PDF/HTML boundary combinations, 63 supported-shape output cases and 12 repeated-output opacity cases. PDFKit/Vision numbering, A4 geometry, links, pixels and partial last pages; relevant pagination/capture/style source unchanged in S3. |
| L1.8 | Render the same configured background image and opacity on index and product pages, behind content without blocking links. | verified | FULL400 plus S3 rerun of the interrupted 1×5 case: 30 DOM count combinations, 75 real PDF/HTML boundary combinations, 63 supported-shape output cases and 12 repeated-output opacity cases. PDFKit/Vision numbering, A4 geometry, links, pixels and partial last pages; relevant pagination/capture/style source unchanged in S3. |
| L1.9 | Prove pagination for 0/1/30/31/60/61 products crossed with 1–5 products per page. | verified | FULL400 plus S3 rerun of the interrupted 1×5 case: 30 DOM count combinations, 75 real PDF/HTML boundary combinations, 63 supported-shape output cases and 12 repeated-output opacity cases. PDFKit/Vision numbering, A4 geometry, links, pixels and partial last pages; relevant pagination/capture/style source unchanged in S3. |
| L1.10 | Cover supported shapes and each partial-page occupancy, including incompatible stored-layout fallback. | verified | FULL400 plus S3 rerun of the interrupted 1×5 case: 30 DOM count combinations, 75 real PDF/HTML boundary combinations, 63 supported-shape output cases and 12 repeated-output opacity cases. PDFKit/Vision numbering, A4 geometry, links, pixels and partial last pages; relevant pagination/capture/style source unchanged in S3. |
| L1.11 | Inspect actual PDF and locally generated standalone HTML artifacts for every index-count boundary and all five products-per-page settings. | verified | FULL400 plus S3 rerun of the interrupted 1×5 case: 30 DOM count combinations, 75 real PDF/HTML boundary combinations, 63 supported-shape output cases and 12 repeated-output opacity cases. PDFKit/Vision numbering, A4 geometry, links, pixels and partial last pages; relevant pagination/capture/style source unchanged in S3. |
| L1.12 | Check page count, A4 dimensions, displayed numbering, link destinations, background presence/opacity, and partial last pages. | verified | FULL400 plus S3 rerun of the interrupted 1×5 case: 30 DOM count combinations, 75 real PDF/HTML boundary combinations, 63 supported-shape output cases and 12 repeated-output opacity cases. PDFKit/Vision numbering, A4 geometry, links, pixels and partial last pages; relevant pagination/capture/style source unchanged in S3. |
| L1.13 | Test absent background and opacity 0, 0.15, and 1. | verified | FULL400 plus S3 rerun of the interrupted 1×5 case: 30 DOM count combinations, 75 real PDF/HTML boundary combinations, 63 supported-shape output cases and 12 repeated-output opacity cases. PDFKit/Vision numbering, A4 geometry, links, pixels and partial last pages; relevant pagination/capture/style source unchanged in S3. |
| L1.14 | Repeat export after editing. | verified | FULL400 plus S3 rerun of the interrupted 1×5 case: 30 DOM count combinations, 75 real PDF/HTML boundary combinations, 63 supported-shape output cases and 12 repeated-output opacity cases. PDFKit/Vision numbering, A4 geometry, links, pixels and partial last pages; relevant pagination/capture/style source unchanged in S3. |
| L1.15 | A successful download event, DOM assertion, or screenshot of the editor alone is not proof of exported-file correctness. | verified | FULL400 plus S3 rerun of the interrupted 1×5 case: 30 DOM count combinations, 75 real PDF/HTML boundary combinations, 63 supported-shape output cases and 12 repeated-output opacity cases. PDFKit/Vision numbering, A4 geometry, links, pixels and partial last pages; relevant pagination/capture/style source unchanged in S3. |

### Verification Execution

| Criterion | Requirement | Status | Evidence / next step |
|---|---|---|---|
| Q.1 | Establish the baseline before changes. | verified | Baseline, FULL400, S3, final verify and catalog-review recorded below. Real IDB injection, blocked external requests, disposable profiles, artifacts and source manifests retained. |
| Q.2 | Run focused tests after each risky change, then npm run verify. | verified | Baseline, FULL400, S3, final verify and catalog-review recorded below. Real IDB injection, blocked external requests, disposable profiles, artifacts and source manifests retained. |
| Q.3 | Fix confirmed failures and rerun affected checks. | verified | Baseline, FULL400, S3, final verify and catalog-review recorded below. Real IDB injection, blocked external requests, disposable profiles, artifacts and source manifests retained. |
| Q.4 | Reuse passing results only while their relevant source state remains unchanged. | verified | Baseline, FULL400, S3, final verify and catalog-review recorded below. Real IDB injection, blocked external requests, disposable profiles, artifacts and source manifests retained. |
| Q.5 | Run the browser regression suite in Chromium, Firefox, and WebKit with representative phone, tablet, and desktop viewports. | verified | Baseline, FULL400, S3, final verify and catalog-review recorded below. Real IDB injection, blocked external requests, disposable profiles, artifacts and source manifests retained. |
| Q.6 | Include widths around the 767/768 and 1199/1200 breakpoints, phone portrait/landscape sizes, and desktop zoom. | verified | Baseline, FULL400, S3, final verify and catalog-review recorded below. Real IDB injection, blocked external requests, disposable profiles, artifacts and source manifests retained. |
| Q.7 | Use real IndexedDB round trips plus deterministic failure/delay injection. | verified | Baseline, FULL400, S3, final verify and catalog-review recorded below. Real IDB injection, blocked external requests, disposable profiles, artifacts and source manifests retained. |
| Q.8 | Do not satisfy persistence requirements using only mocks of successful store updates. | verified | Baseline, FULL400, S3, final verify and catalog-review recorded below. Real IDB injection, blocked external requests, disposable profiles, artifacts and source manifests retained. |
| Q.9 | Exercise the existing export flow from mobile settings/products tabs after editing, and inspect repeated PDF/HTML output for regressions relevant to the changes. | verified | Baseline, FULL400, S3, final verify and catalog-review recorded below. Real IDB injection, blocked external requests, disposable profiles, artifacts and source manifests retained. |
| Q.10 | Keep publishing network calls disabled in tests. | verified | Baseline, FULL400, S3, final verify and catalog-review recorded below. Real IDB injection, blocked external requests, disposable profiles, artifacts and source manifests retained. |
| Q.11 | Record commands, exit codes, expected versus actual outcomes, fixture sizes, browser versions, screenshots/traces, exported artifacts, and source revision or working-tree fingerprint. | verified | Baseline, FULL400, S3, final verify and catalog-review recorded below. Real IDB injection, blocked external requests, disposable profiles, artifacts and source manifests retained. |
| Q.12 | Keep generated artifacts out of tracked source except deliberate small test fixtures. | verified | Baseline, FULL400, S3, final verify and catalog-review recorded below. Real IDB injection, blocked external requests, disposable profiles, artifacts and source manifests retained. |
| Q.13 | Use $catalog-review on the task’s changes before handoff, including new files. | verified | Baseline, FULL400, S3, final verify and catalog-review recorded below. Real IDB injection, blocked external requests, disposable profiles, artifacts and source manifests retained. |
| Q.14 | Address every confirmed task-related finding, rerun affected verification, and review the corrections. | verified | Baseline, FULL400, S3, final verify and catalog-review recorded below. Real IDB injection, blocked external requests, disposable profiles, artifacts and source manifests retained. |
| Q.15 | Separately record unrelated pre-existing issues; do not expand scope to eliminate baseline warnings. | verified | Baseline, FULL400, S3, final verify and catalog-review recorded below. Real IDB injection, blocked external requests, disposable profiles, artifacts and source manifests retained. |

### Real-Device And Family Gate

| Criterion | Requirement | Status | Evidence / next step |
|---|---|---|---|
| R.1 | Actual-device and parent evidence is mandatory. | blocked | Await confirmed devices/versions, PC window/zoom, usual catalog/photo sizes and source-specific technical/each-parent observations; see priority1-device-checks.md. |
| R.2 | The known phones are S22 Ultra, Galaxy Z Flip4, iPhone 15 Pro, and provisionally Moto G32. | blocked | Await confirmed devices/versions, PC window/zoom, usual catalog/photo sizes and source-specific technical/each-parent observations; see priority1-device-checks.md. |
| R.3 | The family also uses laptop/desktop displays described as 14, 15, and 22 inches. | blocked | Await confirmed devices/versions, PC window/zoom, usual catalog/photo sizes and source-specific technical/each-parent observations; see priority1-device-checks.md. |
| R.4 | Obtain confirmation of the actual devices, OS/browser versions, PC viewport/zoom settings, and usual catalog size. | blocked | Await confirmed devices/versions, PC window/zoom, usual catalog/photo sizes and source-specific technical/each-parent observations; see priority1-device-checks.md. |
| R.5 | Use realistic-sized disposable data and representative phone photos. | blocked | Await confirmed devices/versions, PC window/zoom, usual catalog/photo sizes and source-specific technical/each-parent observations; see priority1-device-checks.md. |
| R.6 | On the confirmed devices, verify mobile keyboard behavior, rotation, safe areas, essential controls, saving/reload, and local PDF download/share behavior. | blocked | Await confirmed devices/versions, PC window/zoom, usual catalog/photo sizes and source-specific technical/each-parent observations; see priority1-device-checks.md. |
| R.7 | Do not substitute emulation results for these observations. | verified | Manual guide explicitly separates reported/observed evidence, excludes deferred features and does not substitute emulation. |
| R.8 | Have each parent perform the applicable Priority 1 workflow: change a price/text, replace a photo, move a product, confirm saving and reload, correct an editing mistake or retry a failed save, preview, and produce/check the correct PDF. | blocked | Await confirmed devices/versions, PC window/zoom, usual catalog/photo sizes and source-specific technical/each-parent observations; see priority1-device-checks.md. |
| R.9 | Record device, source version, task outcome, and any help required. | blocked | Await confirmed devices/versions, PC window/zoom, usual catalog/photo sizes and source-specific technical/each-parent observations; see priority1-device-checks.md. |
| R.10 | Blocked tasks, unintended data loss, inaccessible controls, and repeated confusion in essential Priority 1 controls require a scoped fix and retest. | blocked | Await confirmed devices/versions, PC window/zoom, usual catalog/photo sizes and source-specific technical/each-parent observations; see priority1-device-checks.md. |
| R.11 | Do not require later catalog-copy or backup features. | verified | Manual guide explicitly separates reported/observed evidence, excludes deferred features and does not substitute emulation. |
| R.12 | The owner may supply structured manual results when direct observation is unavailable. | verified | Manual guide explicitly separates reported/observed evidence, excludes deferred features and does not substitute emulation. |
| R.13 | Clearly distinguish reported evidence from agent-observed evidence. | verified | Manual guide explicitly separates reported/observed evidence, excludes deferred features and does not substitute emulation. |

## Ongoing requirements

| Criterion | Requirement | Status | Evidence / next step |
|---|---|---|---|
| A5.1 | Shared editable product fields and validation | verified | Shared products/chunks/capture helpers code-reviewed; FULL400 validates all entry paths, shape fallback and actual PDF/HTML; relevant helper source unchanged in S3. |
| A5.2 | Shared page offsets and shape fallback | verified | Shared products/chunks/capture helpers code-reviewed; FULL400 validates all entry paths, shape fallback and actual PDF/HTML; relevant helper source unchanged in S3. |
| A5.3 | Shared PDF/HTML capture preparation | verified | Shared products/chunks/capture helpers code-reviewed; FULL400 validates all entry paths, shape fallback and actual PDF/HTML; relevant helper source unchanged in S3. |
| Q3.1 | Accurate Spanish browser-local saving guidance | verified | README/architecture and this evidence record checked against final source. Spanish browser-local saving/retry guidance; only authorized Playwright package and lockfile additions. |
| Q3.2 | Architecture describes final state and ownership | verified | README/architecture and this evidence record checked against final source. Spanish browser-local saving/retry guidance; only authorized Playwright package and lockfile additions. |
| Q3.3 | Only authorized Playwright dependency changes | verified | README/architecture and this evidence record checked against final source. Spanish browser-local saving/retry guidance; only authorized Playwright package and lockfile additions. |
| REVIEW.1 | catalog-review covers all task hunks and new files | verified | catalog-review covered task changes versus the preserved starting state, including every new file. Four confirmed usability findings fixed, affected checks rerun, corrections reviewed; no unresolved confirmed task-related source finding. |

## Missing external evidence

Owner asked to confirm device models, OS/browser versions, PC viewport/zoom, usual catalog size and availability of both parents for structured manual tests. No reply yet. Real-device and family checks cannot be passed by automation. Need source-specific results including task outcome and help required for each parent.

## Checkpoints

- Baseline: commands above passed; only pre-existing documentation changes. Implementation begins with atomic persistence, honest status and resource ownership. All acceptance checks remain pending.

- Implementation checkpoint: atomic `dbSaveCatalog`, serialized full session revisions, load gate/retry, managed blob URL owners, UUID product IDs, centralized metadata validation, separate management actions, readable Products controls, page offsets/backgrounds, and shared PDF/HTML capture have been implemented. Browser verification is in progress; no full acceptance gate is approved.
- Owner's original ROADMAP/IDEAS content remains byte-identical to baseline; the owner staged these files during this task. No agent staging or commits.
- Installation: npm registry access first failed with ENOTFOUND. Automatic review rejected two installation attempts because it required direct chat authorization. Owner then explicitly authorized Playwright and all three browsers. `npm install --save-dev @playwright/test --ignore-scripts --no-audit --no-fund` passed (exit 0); Playwright 1.63.0 installed Chromium 153.0.8010.12, Firefox 155.0, WebKit 26.6 into `/private/tmp/catalogmaker-playwright-browsers` (exit 0).
- First browser run: `PLAYWRIGHT_BROWSERS_PATH=/private/tmp/catalogmaker-playwright-browsers npm run test -- --project=chromium tests/persistence.spec.ts`, exit 1, 12 passed / 4 failed. All four abort tests found an unhandled second `IDBTransaction.abort()` after the injected abort. Data stayed atomic and retry succeeded, but rejection handling failed. Fixed by handling already-finished aborts; rerun required. Disposable 96×128 PNG fixtures, 1–8 operations. Failure traces/screenshots under `/private/tmp/catalogmaker-priority1-results/` (archived before rerun).
- `npm run verify` after the first capture/UI implementation passed (exit 0), with the same baseline warnings; subsequent fixes require a fresh run.

- **V1 / source S1:** `npm run verify` exit 0 after the database abort/error fixes, accessible controls, capture changes and reorder-animation correction. Relevant source manifest: `/private/tmp/catalogmaker-priority1-source.json`; SHA-256 `619f5f1423ab21398f47a5b96ecd5e8cffd34638f377350cd0d9c3b3aaf0bf0a`, HEAD as above. Includes app source and package/build configuration. Existing warnings unchanged. Later changes require affected reruns.
- **P196 / S1:** three-engine non-artifact run, exit 1: **195 passed, 1 failed** in 4.3 minutes. Evidence archived at `/private/tmp/catalogmaker-priority1-196-run/`. Passed all persistence, mutation/pagination, viewport and WebKit Blob cases, and actual Chromium 200% zoom. The failed Excel test reloaded before awaiting saved; corrected that test precondition (and normal UI/zoom reload preconditions), while keeping the distinct pending-write reload test. Rerun underway. Fixtures: 0/1/30/31/60/61 products, 1–5 per page; 96×128 generated PNGs; 360×800, 800×360, 767/768/1199/1200 widths and 1440×1000. These are disposable synthetic fixtures, not representative phone-photo/catalog-size evidence.
- Earlier private-profile WebKit runs exposed native IndexedDB Blob rejection. A native IDB probe reproduced it independently. Tests now use disposable persistent profiles for all engines; all WebKit cases in P196 passed. This does not establish support for private browsing. Earlier failures retained in `/private/tmp/catalogmaker-priority1-three-engine-first/`.
- **ART / S1 (initial checkpoint):** `PLAYWRIGHT_BROWSERS_PATH=/private/tmp/catalogmaker-playwright-browsers npm run test -- tests/artifacts.spec.ts tests/contrast.spec.ts tests/ownership.spec.ts tests/zoom.spec.ts -g 'actual PDF|opacity|contrast|color control|real Excel|200%'`. 97 selected cases. macOS 26.5 / Node 26.3.0, authorized Playwright versions above. Actual files, native PDFKit/Vision OCR, page dimensions, links, background pixel samples and extracted HTML JPEGs: `/private/tmp/catalogmaker-priority1-artifacts/`. At checkpoint, first 17 Chromium matrix cases passed; no matrix gate approved until final results. Existing system Swift/PDFKit/Vision tools require execution outside the filesystem sandbox; no added tool dependency.
- Review in progress using catalog-review over task changes, including untracked files. Confirmed usability issues to address after the current artifact run: the newly named preview button does not reveal the hidden preview tab on mobile; Excel correction fields disappear as soon as the value reaches the valid limit, losing focus before the user can review the correction. Native drag and repeated HTML opacity/output checks will also be completed before handoff.

- **A11Y1 / S1:** local-server extra checks, exit 1, 7 passed / 11 failed (archive `/private/tmp/catalogmaker-priority1-accessibility-first/`). Keyboard-only edit/photo/move/retry/reset-cancel/export workflow passed in all engines. Confirmed preview photo targets shrink to 18.6px high at 360px and 25.9px at 768px; desktop 1200px passes. Mobile product preview navigation fails in all engines. Drag test failed in Chromium/WebKit; diagnosing valid drop coordinates and native events before attributing this to app code.
- **PADD / S1:** pending-write management serialization for all three effects, partial hydration allocation failure cleanup/retry, and export-lease mutation/allocation gating passed in all engines (15 cases). Combined run exit 1 because two drag cases still failed; archive `/private/tmp/catalogmaker-priority1-persistence-additional/`. These strengthen D1/D2/A3 proof beyond P196.
- **ART diagnostic:** Chromium's 25 PDF/HTML matrix cases, four repeated opacity cases, contrast, color keyboard, corrected Excel and actual zoom cases passed. Firefox PDF checks passed but multiple HTML OCR checks read page 09 as “0g”. A separate crop-and-enlarge OCR probe of the actual extracted JPEG reads “Pág. 09” correctly without supplying expected text. Preserve exact numeric assertions; improve localized OCR after the current run, then repeat affected checks. This is not yet a passing HTML matrix result.

## Detailed proof dimensions

| Subcriterion | Status | Evidence / next action |
|---|---|---|
| D1: delayed startup and StrictMode single read | verified | P196 persistence startup; one coherent read, no early writes, one URL per image. |
| D1: failed read and retry | verified | P196 failed load remains visible without writes or empty adoption. |
| D1: metadata quota / abort | verified | P196 all engines; prior metadata/photo/settings/background bytes preserved. |
| D1: photo quota / abort | verified | P196 all engines; old durable photo retained, draft decodes. |
| D1: settings quota / abort | verified | P196 all engines; atomic rollback and retry. |
| D1: background quota / abort | verified | P196 all engines; atomic rollback and retry. |
| D1: eight overlapping edits / retry plus later edit | verified | P196 latest revision controls status. |
| D1: reload during pending write | verified | P196 complete old or new catalog, never mixed image/metadata state. |
| D1: failed session draft in actual PDF and HTML | verified | S3 export-recovery: actual changed price/photo pixels in PDF and HTML, URL ownership and retry. |
| D2: empty queued behind pending edit | verified | PADD all engines; older completion cannot announce saved. |
| D2: settings reset queued behind pending edit | verified | PADD all engines; preserves metadata/order/photo and restores defaults. |
| D2: full reset queued behind pending edit | verified | PADD all engines. |
| A3: partial URL allocation during hydration | verified | PADD injected second allocation failure: first URL revoked, zero writes; retry decodes all resources. |
| A3: in-progress export retains resource owners | verified | PADD mutations ignored without allocation; post-export action succeeds. |
| A3: failed capture cleanup and subsequent export | verified | S3 export-recovery: rejection, wrapper/body/lease cleanup and successful editing/export afterward. |
| U2: sidebar 16px inputs / 44px primary controls | verified | P196, A11Y1 keyboard walkthrough; computed measurements. |
| U2: scaled preview photo action target | verified | S3 accessibility-extra: measured at least 44px at 360/768/1200 widths in all three engines. |
| U2: secondary targets and state contrast | verified | S3 contrast/secondary-controls: text ≥4.5:1, required control/state contrast ≥3:1, primary ≥44px and secondary ≥24px. |
| U2: actual desktop 200% browser zoom | verified | S3 native Chromium settings: outer/inner ratio 2, CSS zoom 1, visual scale 1. Edit/photo/move/delete/brand/reset-cancel/PDF/reload and full-surface screenshot. |
| U2: family essential-control assessment | blocked | Each parent's source-specific task outcome/help required. |
| U3: native desktop dragging | verified | DRAG: 3/3 engines pass native dragstart/drop/dragend and saved reload using visible source/destination headers. |
| U3/L1: reordered and shrinking refs in actual hook output | verified | S3 export-recovery: actual mobile settings-button and existing handler while Products active; four-page PDF order/links after 31→3 shrink. |
| U5: portrait/landscape viewport and drawer/tab/last-control access | verified | P196 seven viewport sizes and reduced-height resizing. |
| U5: real keyboard open/closed | blocked | Device observations required. |
| U5: real rotation and bottom safe areas | blocked | Device observations required. |
| U5: local system PDF download/share | blocked | Real-device file opened and checked required. |
| L1: absent background / opacity 0 / 0.15 / 1, repeated PDF | verified | FULL400 all engines: real PDF count/edited text/background pixels after each export. |
| L1: absent background / opacity 0 / 0.15 / 1, repeated HTML | verified | FULL400 all engines: actual standalone JPEG OCR and decoded background pixels after each export. |
| L1: supported shapes and all partial occupancies in output | verified | FULL400: 21 supported shapes ×3 engines; capacity-5 page with each 1–5 occupancy, fit and actual PDF/HTML product count/text. |

- **DRAG / S1:** exit 0, 3 passed. Corrected the test to drag between visible card headers; earlier downward target was clipped by the scroll panel. Native event attachments and reload proof at `/private/tmp/catalogmaker-priority1-native-drag-pass/`. No app drag changes were necessary.

- **SECONDARY / S1:** exit 0, 6 passed; archive `/private/tmp/catalogmaker-priority1-secondary-pass/`. Tests measure text on the elements that paint it after CSS transitions settle, composite gradient/background colors, check selected layout border ≥3:1, text ≥4.5:1, secondary controls ≥24px and labels/pressed/selected states. They also check closing the color panel after a viewport-height change; this is not real-keyboard proof. Earlier measurements incorrectly used an unpainted parent color and transient transition colors; corrected measurement methodology without lowering thresholds. Primary controls retain the separate 44px requirement.
- **EXTRA ART / S1 (initial checkpoint):** all 21 supported shapes, each 1–5 occupancy on capacity-5 pages, actual failed-draft PDF/HTML photo/text, capture-failure cleanup and actual mobile PDF-hook output after reorder/shrinking refs. Files saved under the artifact root with `shape-*`, `failed-draft`, and `mobile-hook-*` names. Firefox's failed-draft PDF visibly contains `$79999` but whole-page OCR reads the isolated price upside down; localized image-region recognition is needed. Numeric expected values remain unchanged.
- New/untracked source, tests and docs passed explicit trailing-whitespace/final-newline inspection. ROADMAP.md and IDEAS.md contents remain byte-identical to the preserved user baseline.

- **EXTRA ART / S1 result:** exit 1, **70 passed / 2 failed** in 3.2 minutes. All 63 shape-output cases, all three capture-cleanup cases and all three real mobile-hook reordered/shrinking-ref cases passed. Failed-draft PDF/HTML passed in Chromium; Firefox/WebKit stop at whole-page price OCR before HTML generation. Native rendered PDF visually shows correct `$79999`; the improved footer-region OCR independently reads that exact price. Archive `/private/tmp/catalogmaker-priority1-extra-art-first/`. Re-run both affected cases after adopting localized OCR.

- WebKit 61×1 ART case generated and passed all PDF and standalone HTML file checks, then timed out evaluating cleanup on the original background tab after closing the standalone viewer. Trace shows no crash event; both capture calls completed (~51s PDF, ~53s HTML). Move the capture-wrapper assertion directly after capture, before opening the viewer, and explicitly restore the original tab. Repeat this case without lowering output assertions or treating the timeout as a pass.

## Historical source checkpoint S2

App/package/build manifest: `/private/tmp/catalogmaker-priority1-s2-app-manifest.json`, SHA-256 `a79014156521246695e3f68849cd9c664c51c5d1e0b1bd128a9f7028530917d2`.
Full app/test/config manifest: `/private/tmp/catalogmaker-priority1-s2-full-manifest.json`, SHA-256 `5b9daba2108d243ee45ac8547f3f49ec10d7e9670a7cc1934c53fdbb21e9cfac`.

- Catalog-review checkpoint found three confirmed issues: mobile preview navigation/focus, disappearing valid Excel correction field, and scaled preview photo targets below 44px. Fixed all three. Navigation now reveals the preview and focuses its named product group; index navigation shares the same focus helper. Correction fields remain editable through confirmation/cancellation. Preview photo buttons compensate for A4 scaling and are removed before export. Reviewed all eight correction files against preserved S1 copies; no additional confirmed task-related source finding.
- **CORRECTIONS:** `PLAYWRIGHT_BROWSERS_PATH=/private/tmp/catalogmaker-playwright-browsers npm run test -- --workers=2 tests/accessibility-extra.spec.ts tests/ownership.spec.ts tests/artifacts.spec.ts tests/export-recovery.spec.ts tests/zoom.spec.ts -g '44px|mobile product preview|real Excel|opacity|failed photo|200%'`, exit 0, **31 passed** in 53.8s. Archive `/private/tmp/catalogmaker-priority1-corrections-pass/`. All three engines pass corrected targets, navigation/focus, Excel correction and actual failed-draft PDF/HTML photo/text. Actual repeated HTML opacity pixels and edited text now checked alongside PDFs. Broader native-zoom workflow passes photo replacement, move/delete, brand text, cancellation of all resets and actual PDF count/price inspection.
- `npm run verify` after S2 source changes: exit 0, existing Vite/bundle warnings only. S2 code remained unchanged through FULL400; S3 has a separate verification result below.
- Localized OCR reads independent header/footer image crops at larger resolution; no expected number/price is supplied to OCR. Exact numeric assertions are retained. Capture-wrapper cleanup is asserted immediately after capture, before opening the standalone viewer; original tab is brought forward before closing the viewer.
- **FULL400 (start checkpoint):** `PLAYWRIGHT_BROWSERS_PATH=/private/tmp/catalogmaker-playwright-browsers npm run test -- --workers=2`, 400 cases, all engines, no skips requested. Per-worker native inspector binaries prevent compiler collisions. Await final exit code and any affected reruns before approving the automated gate.
- Additional optional static test-code check (`tsc --noEmit --strict ... tests/*.ts playwright.config.ts`) could not complete because Node type declarations are not installed. One actual IDBKeyRange annotation issue it exposed was corrected. Runtime suite transpiles and runs independently; app TypeScript/build passes. No additional dependency was installed beyond the explicitly authorized Playwright packages. This optional check is not reported as passing.

- Supplementary native zoom capture uses Chromium's full surface screenshot API because Playwright's ordinary screenshot rectangle is cropped after changing native zoom. `/private/tmp/catalogmaker-priority1-artifacts/native-zoom-200-full.png` records the full viewport; geometry: outer 1440px, inner 720×456 CSS px, devicePixelRatio 2, visual scale 1, CSS zoom 1. The automated native-settings workflow remains the behavioral proof and checks its real PDF.
- Asked the owner for S2 manual-test devices, OS/browser versions, PC window/zoom, usual catalog size, availability of both parents, and whether phones share this Mac's local network. Manual access setup and representative-data testing depend on that answer; no catalog was published.

- Fourth confirmed review finding (historical S2, fixed and retested in S3): an open color popup is outside the editor fieldset. Starting PDF export with the keyboard leaves its inputs enabled; entering HEX `00FF00` is displayed locally while the catalog remains white and reports saved because the export lease correctly ignores mutation. A disposable browser probe reproduced the mismatched displayed/stored value. `popup-busy.spec.ts` now reproduces the actual keyboard-started export sequence for product and page colors. Prepared a scoped fix that passes the existing busy state into both popup callers and closes/hides the popup while busy; application source remains frozen until FULL400 finishes. This finding is not treated as passing merely because the existing suite passes.

## Final relevant source S3 and current results

Latest evidence covers **406 distinct passing cases**: 112 rerun on S3 plus 294 unaffected passing cases retained from FULL400. The five FULL400 failures all have passing S3 reruns. This is combined evidence, not a claim that the original 400-case command exited successfully. Per-case mapping: `/private/tmp/catalogmaker-priority1-final-coverage.json`.

App/package/build manifest: `/private/tmp/catalogmaker-priority1-s3-app-manifest.json`, SHA-256 `657d12d29a21058613e0c23a7cce0a93fa14aeafecdba84de86718278237302d`.
Full app/test/config manifest: `/private/tmp/catalogmaker-priority1-s3-full-manifest.json`, SHA-256 `20a0d6cf51ec9ea61b5c73139919b4775441ef5126a3afc4d95da1d8febfba70`.

- **FULL400 / S2:** `PLAYWRIGHT_BROWSERS_PATH=/private/tmp/catalogmaker-playwright-browsers npm run test -- --workers=2`, exit 1, **395 passed / 5 failed**, zero flakes/skips, 55.7 minutes. Original full JSON report and attachments: `/private/tmp/catalogmaker-priority1-full400-first/`. Passed the formerly failing Firefox localized-OCR matrix and WebKit 61×1 cleanup sequence. Three failures were the legacy-load test consulting its harness after reload without reattaching it; fixed the test setup and repeated all three. Two WebKit cases (1×5 artifact and keyboard-move context setup) experienced a simultaneous approximately 17-minute interruption; its underlying cause is unconfirmed. Both were repeated with unchanged timeouts and assertions in S3.
- **S3:** product/page color popups now close and become unavailable during export or management; all other application changes from S2 are preserved. Three application source files changed: GradientPickerPopover and its active ColorGroup/ProductListItem callers. No storage, capture, pagination, page styling or output algorithm changed; FULL400 results for those paths remain relevant. Test-only changes reattach the legacy-load harness after its deliberate failed-load reload and capture the full native-zoom surface. Four S2 files preserved at `/private/tmp/catalogmaker-priority1-s2-to-s3/` for correction review.
- **Popup reproduction:** both new Chromium regression cases failed against S2 because the dialog remained editable after keyboard-started PDF export; archive `/private/tmp/catalogmaker-priority1-popup-first/`. The S3 regression checks close-on-export, a real PDF download, reopening/editing and actual committed color afterward in all engines.
- **S3 affected checks:** command recorded in `/private/tmp/catalogmaker-priority1-s3-command.txt`, exit 0, **112 passed**, zero failures/flakes/skips, 1.8 minutes. Report, actual popup/zoom PDFs, full native-zoom screenshot, dimensions and measurement attachments: `/private/tmp/catalogmaker-priority1-s3-pass/`. Repeats UI, keyboard/native reorder, contrast/secondary controls, viewport/preview access, ownership, queued management/export locks, real failed-draft/mobile-hook output and native zoom.
- Final `npm run verify`: exit 0 (app TypeScript, Vite build and Git whitespace checks). Explicit untracked-file whitespace/newline check also passes. Existing Vite CJS and bundle-size warnings remain; no general dependency/security audit or publishing verification performed. The optional standalone test TypeScript check remains unavailable without Node type declarations; no additional package installed.
- Final catalog-review: ordinary handoff scope against initial working-tree baseline, including new source/tests/config/docs. All four confirmed usability findings are fixed and the corrections reviewed; no unresolved confirmed task-related source finding. This does not certify unobserved device/family behavior. ROADMAP.md and IDEAS.md still match the preserved user content; their staging was user-owned. No commit, push or publication.

Exact S3 affected-check command:

```sh
PLAYWRIGHT_BROWSERS_PATH=/private/tmp/catalogmaker-playwright-browsers npm run test -- --workers=2 tests/accessibility-extra.spec.ts tests/contrast.spec.ts tests/secondary-controls.spec.ts tests/ui.spec.ts tests/ownership.spec.ts tests/persistence-additional.spec.ts tests/export-recovery.spec.ts tests/popup-busy.spec.ts tests/zoom.spec.ts tests/mutations-pagination.spec.ts tests/artifacts.spec.ts --grep-invert 'actual PDF and HTML (?!1 products / 5 per page)|repeated actual output|pagination [0-9]+ products|fixed clock mixed|central limits reject|every supported shape'
```

Actual exported files remain in `/private/tmp/catalogmaker-priority1-artifacts/`: all 75 boundary PDFs and standalone HTML files, rendered PDF pages, decoded HTML JPEGs, per-file inspection JSON, all supported shape outputs, repeated opacity outputs and failed-draft/mobile-hook examples. Automated checks inspect files, not just download events. Visual spot-checks include Firefox's actual page 16 with the lone product 061 and its configured background; the 61×1 outputs contain 64 A4 pages. S3 full-surface native zoom screenshot replaces the cropped ordinary Playwright screenshot; it waits for the selected tab and finite transitions to settle.

Secondary controls were assessed against the [W3C target-size guidance](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html): measured 24px targets or larger; the objective separately requires 44px primary controls. These are scoped measurements, not whole-app WCAG certification.

| Priority ID | Current automated evidence | Remaining mandatory evidence |
|---|---|---|
| D1 | Atomic load/save/error/retry, every persistence domain, overlapping edits and reload; failed-draft PDF/HTML. | Confirmed-device saving/reload and each parent's understanding of saved/error state. |
| D2 | Three distinct serialized resets, cancellation/failure/retry and byte-preserving settings reset; background tabs/removal. | Technical checks on confirmed devices with disposable data. |
| A3 | Durable/draft/pending/export ownership, URL cleanup and real IDB/photo decoding under failures. | Realistic catalog size and representative phone-photo observations on confirmed devices. |
| A2 | UUID intake, invalid/no-op mutations, shared 500-character limits and correctable Excel import. | Representative device/family editing and photo intake. |
| U2 | Labels/states/focus, input/target/contrast measurements and actual 200% browser zoom. | U2.13: both-parent assessment, confirmed PC settings and device observations. |
| U3 | Keyboard and native drag reorder, useful focus, persisted order and actual output agreement. | Each parent moves a product and checks the result on a confirmed device. |
| U5 | Phone/tablet/desktop emulation, breakpoints, drawer/tabs, resizing and last actions. | U5.4: real keyboard, rotation, safe areas, local download/share; both-parent workflow. |
| L1 | Full actual PDF/HTML boundary, shape, opacity and repeated-output matrix. | Correct PDF opened/checked on confirmed devices and by each parent. |

**Exact remaining gate:** R.1–R.6 and R.8–R.10, including U2.13 and U5.4. No real-device or parent result has been supplied or observed. The smallest next input is confirmation of available devices and versions, PC window/zoom, usual catalog/photo sizes, and availability of both parents (plus shared local-network access if these phones will test this Mac's version). Then use `priority1-device-checks.md` for source-specific results and fix/retest any reported Priority 1 failure. Missing evidence remains blocked; the goal is not complete.
