export interface Product {
  id: string;
  name: string;
  price: string;
  description: string;
  image: string;
  bgColor: string;
}

export interface Colors {
  bg: string;
  primary: string;
  secondary: string;
  text: string;
}

export interface Fonts {
  heading: string;
  body: string;
  small: string;
}
